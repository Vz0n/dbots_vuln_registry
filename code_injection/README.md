This class of bugs follows the same pattern: somebody manages to inject any type of code by any type of means (injected system commands or unsafe deserialization are two examples) and then, they get command execution as the user who is running the application process.

Generally, those bugs are automatically between $[9,10]$ in the CVSS scoring if it can be done by normal users.   